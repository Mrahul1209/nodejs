<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Book Edit Form</title>
    <!-- Bootstrap 5 CDN -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

</head>

<body>
    <h1 style="text-align: center;" class="mt-5">Update Book</h1>

    <div class="container col-4">
        <form method="post" action="/updateBook" enctype="multipart/form-data">
            <div class="mb-3">
                <input type="hidden" name="id" value="<%= book._id %>" class="form-control">
            </div>
            <div class="mb-3">
                <label class="form-label">Book Name</label>
                <input type="text" name="book_name" value="<%= book.book_name %>" class="form-control" required>
            </div>

            <div class="mb-3">
                <label class="form-label">Author</label>
                <input type="text" name="book_author" value="<%= book.book_author %>" class="form-control" required>
            </div>

            <div class="mb-3">
                <label class="form-label">Price</label>
                <input type="number" name="book_price" value="<%= book.book_price %>" class="form-control" required>
            </div>

            <div class="mb-3">
                <label class="form-label">Language</label>
                <input type="text" name="book_language" value="<%= book.book_language %>" class="form-control" required>
            </div>

            <div class="mb-3">
                <label class="form-label">Image</label>
                <input type="text" name="book_image" value="<%= book.book_image %>" class="form-control">
            </div>

            <button type="submit" class="btn btn-warning">Update Book</button>
        </form>


    </div>


    <!-- Bootstrap JS (for future buttons/modals) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>